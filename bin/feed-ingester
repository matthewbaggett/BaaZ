#!/usr/bin/php
<?php

require_once(__DIR__ . "/../vendor/autoload.php");

use Baaz\Workers;
use ⌬\⌬;

⌬::Instance([
    'config' => __DIR__ . "/../.benzine.yml",
])
    ->getContainer()
    ->get(Workers\FeedIngester::class)
    ->run();